{
  "name": "streamview",
  "version": "1.0.0",
  "description": "Twitch & YouTube Streaming tools",
  "main": "server.js",
  "scripts": {
    "start": "node server.js",
    "build": "npm install && python3 --version && pip3 --version && pip3 install -r requirements.txt --verbose"
  },
  "dependencies": {
    "axios": "^1.7.7",
    "bcryptjs": "^2.4.3",
    "cookie-parser": "^1.4.7",
    "dotenv": "^16.4.5",
    "express": "^4.21.1",
    "express-rate-limit": "^7.4.1",
    "mongoose": "^8.7.2",
    "node-cron": "^4.1.0"
  },
  "engines": {
    "node": "18.x"
  }
}
